# Railway configuration for PDF service
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/src/builder-io-react-test/Dockerfile"
dockerfileContext = "."

[deploy]
startCommand = "serve -s spa -l 8000"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
